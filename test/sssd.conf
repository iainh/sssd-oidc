[sssd]
services = nss, pam
domains = oidc

[domain/oidc]
id_provider = proxy
proxy_lib_name = oidc
auth_provider = proxy
proxy_pam_target = oidc
entry_cache_timeout = 300

[nss]
default_shell = /bin/bash
fallback_homedir = /home/%u
