[package]
name = "pam-oidc"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "GPL-2.0-or-later"
description = "PAM module for OIDC authentication"

[lib]
name = "pam_oidc"
crate-type = ["cdylib", "rlib"]

[dependencies]
libc = "0.2"
sssd-oidc = { path = "../.." }
tracing = "0.1"
